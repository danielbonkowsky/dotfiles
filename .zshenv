# Sets up Rust/Cargo environment variables and paths
. "$HOME/.cargo/env"

# Adds Ruby gems (packages) to $PATH to make them executable globally
export PATH=$HOME/.gem/bin:$PATH

# Add Protoc to $PATH
export PATH=/Users/danielbonkowsky/robocup/protoc-29.2-osx-aarch_64/bin:$PATH
